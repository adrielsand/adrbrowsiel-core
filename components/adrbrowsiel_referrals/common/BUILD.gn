import("//adrbrowsiel/components/adrbrowsiel_referrals/buildflags/buildflags.gni")

assert(enable_adrbrowsiel_referrals)

source_set("common") {
  sources = [
    "pref_names.cc",
    "pref_names.h"
  ]
}
