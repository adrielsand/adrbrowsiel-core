module adrbrowsiel_search.mojom;

import "mojo/public/mojom/base/values.mojom";

interface adrbrowsielSearchFallback {
  FetchBackupResults(string query_string,
                     string lang,
                     string country,
                     string geo,
                     bool filter_explicit_results) => (string response);
};
